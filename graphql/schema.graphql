type Movie {
  id: String!
  name: String!
  score: Int!
}

type Query {
  movies: [Movie]!
  movie(id: String!): Movie
}

type Mutation {
  addMovie(name: String!, score: Int!): Movie!
  deleteMovie(id: String!): Boolean!
}
